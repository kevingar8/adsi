doctype
html(lang="es")
	head
		meta(charset="UTF-8")
		title Atributos (Estilo)
	body 
		// Estilos en los atributos 
		h1(style={background:"crimson", color:"white", padding:"20px"}) Atributos (Estilo)
		// Atributos booleanos
		label(for="enclase")
			span Usted esta en Clase ?
			input(type="checkbox" checked=true id="enclase") 
		// Variables Atributos
		- var colores = ["yellow", "blue", "red"]
		a(class=colores) Colombia